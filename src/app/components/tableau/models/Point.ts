/**
 * Represents an x/y coordinate in pixels.
 *
 * @see https://help.tableau.com/current/api/embedding_api/en-us/reference/interfaces/point.html
 */
export interface Point {
  /** X coordinate of point. */
  x: number;

  /** Y coordinate of point. */
  y: number;
}
